import 'package:flutter/material.dart';

class TabTurfs extends StatelessWidget {
  const TabTurfs({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Turfs'),
      ),
    );
  }
}
